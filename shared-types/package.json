{
  "name": "@servicehub/shared-types",
  "version": "1.0.0",
  "description": "Shared TypeScript types for ServiceHub platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist || rmdir /s /q dist 2>nul || true"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "files": [
    "dist/**/*"
  ]
}
