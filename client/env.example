# ==============================================
# CLIENT ENVIRONMENT CONFIGURATION
# ==============================================
# Copy this file to .env and fill in your values

# API Backend URL
# For local development:
VITE_API_URL=http://localhost:4000

# For production (Render):
# VITE_API_URL=https://platform-api.onrender.com

# For production (Railway):
# VITE_API_URL=https://your-app.up.railway.app

# ==============================================
# NOTES
# ==============================================
# 
# Vite Environment Variables:
# - Must be prefixed with VITE_ to be exposed to client
# - Loaded at build time, not runtime
# - Rebuild required after changes
#
# Development:
# - Uses Vite proxy to avoid CORS issues
# - Can leave VITE_API_URL empty (uses proxy)
#
# Production:
# - Must specify full backend URL
# - Ensure CORS is configured on backend

